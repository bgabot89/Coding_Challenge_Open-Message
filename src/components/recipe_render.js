import React, { Component } from 'react';
import FoodList from '../containers/food_list';

//component is supposed to show a recipe when the user inputs a food and then reroutes to this specific component
export default class RecipeRender extends Component {
  render () {
    return (
      <div>
        <h1> Recipe </h1>
      </div>
    );
  }
};
